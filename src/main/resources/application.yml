spring:
  application:
    name: SPRING-BOOT-MALL
  profiles:
    active: dev               
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH-mm-ss  
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: HTML     
springdoc:
  swagger-ui:
    path: /swagger-ui
